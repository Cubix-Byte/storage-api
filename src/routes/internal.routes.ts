import { Router } from "express";

/**
 * Internal API Routes
 * 
 * Routes for microservice-to-microservice communication
 * Requires x-api-key header for authentication (handled by global auth middleware)
 * Follows /storage/api/v1/internal/... pattern
 */
const router = Router();

// Internal routes will be added here as needed

export default router;

